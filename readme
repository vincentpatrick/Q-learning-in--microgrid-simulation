

the files on all experiments can be run using google colab.
Everyday, the dataset changes when using the pymgrid package.
if the training of the agent encounters an error, simply decrease the number of horizon set for the agents training


This baseline and experimental algorithm is built based on the Q-learning algorithm made by Total-RD.
The link to TotalRD's code is: https://github.com/Total-RD/pymgrid
